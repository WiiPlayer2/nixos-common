{ inputs, ... }:
{
  programs = {
    # sway.enable = true;
    niri.enable = true;
  };

  # services.displayManager = {
  #   dms-greeter = {
  #     enable = true;
  #     compositor.name = "niri";
  #   };
  # };
}
